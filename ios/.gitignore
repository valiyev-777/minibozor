# testflight.sh writes the archive, export and ExportOptions.plist here.
build/
*.xcarchive
xcuserdata/
*.xcuserstate

# Local App Store Connect identifiers — see testflight.env.example
testflight.env
