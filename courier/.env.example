# Where the FastAPI backend is. Read at build time by Vite.
# On a handset this has to be an address the phone can reach — the
# laptop's LAN address, or localhost through `adb reverse tcp:8000 tcp:8000`.
VITE_API_URL=http://localhost:8000
