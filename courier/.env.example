# Where the FastAPI backend is. Read at build time by Vite.
#
# Port 8001, not 8000: the customer app's dev server owns 8000 and both are
# usually running at once.
VITE_API_URL=http://localhost:8001
