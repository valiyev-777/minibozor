__pycache__/
*.pyc
.venv/
.env
*.db
# Database backups. `cp minibozor.db minibozor.db.bak` before a downgrade is the
# documented habit (see README, Schema changes) and the result is somebody's real
# catalogue and orders — never source.
*.db.bak
*.db-journal
*.db-wal
.pytest_cache/

# dev server output
server.log

# Runtime uploads. What people send through /staff/media is data the
# deployment holds, not source the repository ships; the seeded catalogue
# pictures beside it are.
media/uploads/
